rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // v1: any signed-in player can read/write game docs.
    // Tighten per-collection before opening to strangers.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
